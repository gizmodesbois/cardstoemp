<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">
    <head>
		<meta name="robots" content="noindex, nofollow">
		<title>{{title}}</title>
    </head>
	<body>
		<main>
			{{ layoutContent | safe }}
		</main>
	</body>
</html>
